vector<int> prefix_function(string s) {
    int n = (int)s.length();
    vector<int> pi(n);
    for (int i = 1; i < n; ++i) {
        int j = pi[i - 1];
        while (j > 0 && s[i] != s[j])
            j = pi[j - 1];
        if (s[i] == s[j]) ++j;
        pi[i] = j;
    }
    return pi;
}

Префикс-функция π[i] — это длина наибольшего собственного суффикса подстроки s[0..i], который одновременно является префиксом всей строки.

Проще говоря: π[i] = длина наибольшего префикса строки, который является суффиксом для s[0..i].
